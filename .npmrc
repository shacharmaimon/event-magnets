# Use CrowdStrike's Artifactory npm mirror for THIS project.
# The public registry (registry.npmjs.org) is blocked by the corporate Zscaler
# proxy; Artifactory is an allowed internal mirror that proxies public npm.
# npm-prod-virtual is a general mirror (mirrors the full public registry).
registry=https://artifactory.cicd.dc/artifactory/api/npm/npm-prod-virtual/
